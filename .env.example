# Copy to `.env` and edit. Compose reads `.env` automatically, so every
# `docker compose` command in this directory picks these up — unlike a one-off
# `VAR=x docker compose up`, which applies to that single command and lets the
# NEXT one recreate the container without it.
#
# `.env` is gitignored. This example file is not, so it must never hold a real
# secret.

# eiermann's port, everywhere. federfall's dev stack owns 8090, and the app's
# development defines (apps/eiermann/dart_defines/development.json) point at
# 8091 — keep the two in agreement or the app cannot find its own backend.
EIERMANN_PORT=8091

# The first user. Without these a fresh instance has no way in: a PocketBase
# superuser is not an app-level coordinator. Idempotent, and also the
# lockout-recovery path. Clear them once the team exists.
EIERMANN_COORDINATOR_EMAIL=dev@eiermann.local
EIERMANN_COORDINATOR_PASSWORD=DevPass12345!
EIERMANN_COORDINATOR_NAME=Koordination

# ── Sign-in through an identity provider (optional) ──────────────────────
#
# Email plus password stays the default and needs nothing here. Set these only
# for a group that HAS an identity provider; `zv_settings.js` configures a
# provider the moment its id and secret are both present, and /api/eiermann/info
# then advertises it so the app can show the button.
#
# `<NAME>` is the provider id, upper-cased: OIDC for a generic OpenID Connect
# server, or a social one PocketBase knows (GOOGLE, MICROSOFT, …). A social
# provider needs only the id and the secret; a generic one needs the three URLs
# as well.
#
# EIERMANN_OAUTH2_PROVIDERS=oidc
# EIERMANN_OAUTH2_OIDC_CLIENT_ID=eiermann
# EIERMANN_OAUTH2_OIDC_CLIENT_SECRET=
# EIERMANN_OAUTH2_OIDC_DISPLAY_NAME=Anmeldung der Gruppe
# EIERMANN_OAUTH2_OIDC_AUTH_URL=https://id.example.org/authorize
# EIERMANN_OAUTH2_OIDC_TOKEN_URL=https://id.example.org/token
# EIERMANN_OAUTH2_OIDC_USERINFO_URL=https://id.example.org/userinfo
# Leave PKCE unset unless the provider refuses it: the default is the one
# PocketBase picks per provider, and writing it explicitly overrides that.
# EIERMANN_OAUTH2_OIDC_PKCE=true
#
# WHO becomes WHAT is the other half. Somebody signing in for the first time gets
# an account, and unless a group below maps them, that account is a `guest`:
# authenticated, and refused by every access rule until a coordinator lets them
# in. Leave the three unset and that is the whole behaviour — the identity
# provider says who somebody is, a person still decides what they may do.
#
# Set them to map the provider's groups straight onto roles. The privilege order
# is fixed in the hook (coordinator before member) and the first match wins.
#
# EIERMANN_OIDC_COORDINATOR_GROUP=tauben-koordination
# EIERMANN_OIDC_MEMBER_GROUP=tauben-team
# Which claim the groups arrive in. Default: groups.
# EIERMANN_OIDC_GROUPS_CLAIM=groups
#
# The gate, for an instance whose provider holds far more people than the pigeon
# group: with this set, an account in none of these groups is refused
# registration outright instead of landing as a guest. It applies to the very
# first account too.
# EIERMANN_OIDC_ALLOWED_GROUPS=tauben-koordination,tauben-team
#
# Only for a private provider that never sends `email_verified`, and only if you
# trust it: without this an address the provider did not verify still lands in
# the roster, just not marked verified.
# EIERMANN_OIDC_TRUST_EMAIL=true
#
# For a local end-to-end try-out there is a mock provider:
# `docker-compose.oidc.yml`.

# ── The geocoder ─────────────────────────────────────────────────────────
#
# Both of these want setting before a real deployment, and neither does
# anything in development. Empty means the public OSM Nominatim under the
# User-Agent "eiermann/1.0", and that instance blocks application backends and
# placeholder agents — so address lookup works on your laptop and stops the day
# somebody else uses it. Self-host, or put a real contact in the agent.
#
# EIERMANN_NOMINATIM_URL=https://nominatim.example.org
# EIERMANN_USER_AGENT=eiermann/1.0 (tauben@example.org)

# ── The map source ───────────────────────────────────────────────────────
#
# ALL OR NOTHING: /api/eiermann/info refuses a partial set and the app falls
# back to its build-time defines, because a half-applied override naming the
# wrong provider is a licensing problem. Set the mode, the URL for that mode,
# and the credit the provider requires.
#
# EIERMANN_MAP_MODE=raster
# EIERMANN_MAP_TILE_URL=https://tiles.example.org/{z}/{x}/{y}.png
# EIERMANN_MAP_ATTRIBUTION=© OpenStreetMap-Mitwirkende
# EIERMANN_MAP_ATTRIBUTION_URL=https://www.openstreetmap.org/copyright
